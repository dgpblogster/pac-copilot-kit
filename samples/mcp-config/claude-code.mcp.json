{
  "//": "Claude Code: save as .mcp.json at the repo root of the project that uses the kit. Requires the PacCopilotKit module: Install-Module PacCopilotKit",
  "mcpServers": {
    "pac-copilot-kit": {
      "command": "npx",
      "args": ["-y", "pac-copilot-kit-mcp"],
      "env": {
        "PCK_DEFAULT_ENVIRONMENT_ID": "00000000-0000-0000-0000-000000000000"
      }
    }
  }
}
