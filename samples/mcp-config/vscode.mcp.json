{
  "//": "VS Code (GitHub Copilot): save as .vscode/mcp.json in the workspace. Requires the PacCopilotKit module: Install-Module PacCopilotKit",
  "servers": {
    "pac-copilot-kit": {
      "command": "npx",
      "args": ["-y", "pac-copilot-kit-mcp"],
      "env": {
        "PCK_DEFAULT_ENVIRONMENT_ID": "00000000-0000-0000-0000-000000000000",
        "PCK_WORKSPACE_ROOT": "${workspaceFolder}"
      }
    }
  }
}
